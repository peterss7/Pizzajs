import { styled } from "@mui/system";

export const AnnexInputWrapper = styled("div")(() => ({
    position: "relative",
}));


export const AnnexTextInput = styled("input")(() => ({
    position: "absolute",
    left: 52,
    bottom: 102,
    width: 300,
}));


export const AnnexActionButtons = styled("input")(() => ({
    marginTop: 6,
    fontSize: 12,
    color: "rgba(255,255,255,0.7)"
}));

